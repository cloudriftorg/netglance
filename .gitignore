# Build artifacts
/netglance
backend/netglance
frontend/dist/
frontend/node_modules/
frontend/tsconfig.tsbuildinfo
# Frontend build artifacts copied here by `make build` / Dockerfile.
# Keep the placeholder index.html in git so //go:embed has files to scan
# in CI (go vet / go test) when no frontend build has run.
backend/internal/webui/dist/*
!backend/internal/webui/dist/index.html

# Dev data
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Editor / OS
.DS_Store
.idea/
.vscode/
*.swp

# Env
.env
.env.local
